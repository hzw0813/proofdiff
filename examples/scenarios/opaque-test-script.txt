ProofDiff  Evidence for this change
────────────────────────────────────────────────────────────────────────────────────────────────────
access-control  working tree vs HEAD  @ 4f059fcf9ab5

PARTIAL  ·  highest risk MEDIUM  ·  1 file  ·  1 symbol
0 qualified target passes  1 partial  0 unverified  0 unknown  0 failed

PARTIAL    src/access.js  MEDIUM 37
  modified, +1/-1  ·  canDelete
  Review: Evidence exists but is not connected to a related passing test. Security-sensitive path name warrants focused human review.
  Evidence: test: test — Repository test command passed, but ProofDiff did not observe which test files it executed.
  Boundary: runner-qualification · opaque-passing-check · fail-closed — A repository command passed, but no related test-like path was qualified as an exact target for a recognized runner.
  Next: Use a supported runner convention or explicit runner target so ProofDiff can bind runtime observation to the related test file.
  Test-like paths: test/access.test.js (not observed passing)

Checks
  PASS      test: test  package.json script "test"  143ms

Trust boundary: Repository-defined checks were executed because --run-checks was explicitly supplied. Output was bounded, the repository root and common secret patterns were redacted; this is not an operating-system sandbox.

Related test file passed requires runner qualification, explicit supply, and a non-skipped passing test observed for that exact target. This is not changed-symbol or changed-line coverage and does not show that behavior is correct. Declared-commit-matched LCOV, when supplied, is separate additive coverage evidence reporting artifact hits; ProofDiff does not independently attest artifact provenance, assertion relevance, or correctness.
